{
  "scripts": {
    "test": "RETRY_COUNT=30 npx playwright test --headed",
    "test-ci": "RETRY_COUNT=30 npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.16.3",
    "@types/jest": "^27.0.3",
    "dotenv": "^10.0.0"
  }
}
